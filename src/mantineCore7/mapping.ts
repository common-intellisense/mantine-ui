import Accordion from './Accordion.json'
import AccordionControl from './AccordionControl.json'
import AccordionItem from './AccordionItem.json'
import AccordionLabel from './AccordionLabel.json'
import AccordionPanel from './AccordionPanel.json'
import ActionIcon from './ActionIcon.json'
import ActionIconGroup from './ActionIconGroup.json'
import Affix from './Affix.json'
import Alert from './Alert.json'
import Anchor from './Anchor.json'
import AppShell from './AppShell.json'
import AppShellAside from './AppShellAside.json'
import AppShellFooter from './AppShellFooter.json'
import AppShellHeader from './AppShellHeader.json'
import AppShellNavbar from './AppShellNavbar.json'
import AspectRatio from './AspectRatio.json'
import Autocomplete from './Autocomplete.json'
import Avatar from './Avatar.json'
import BackgroundImage from './BackgroundImage.json'
import Badge from './Badge.json'
import Blockquote from './Blockquote.json'
import Box from './Box.json'
import Breadcrumbs from './Breadcrumbs.json'
import Burger from './Burger.json'
import Button from './Button.json'
import ButtonGroup from './ButtonGroup.json'
import Card from './Card.json'
import CardSection from './CardSection.json'
import Center from './Center.json'
import Checkbox from './Checkbox.json'
import CheckboxCard from './CheckboxCard.json'
import CheckboxGroup from './CheckboxGroup.json'
import CheckboxIndicator from './CheckboxIndicator.json'
import Chip from './Chip.json'
import ChipGroup from './ChipGroup.json'
import CloseButton from './CloseButton.json'
import Code from './Code.json'
import Collapse from './Collapse.json'
import ColorInput from './ColorInput.json'
import ColorPicker from './ColorPicker.json'
import ColorSwatch from './ColorSwatch.json'
import Combobox from './Combobox.json'
import ComboboxDropdown from './ComboboxDropdown.json'
import ComboboxDropdownTarget from './ComboboxDropdownTarget.json'
import ComboboxEventsTarget from './ComboboxEventsTarget.json'
import ComboboxGroup from './ComboboxGroup.json'
import ComboboxOption from './ComboboxOption.json'
import ComboboxSearch from './ComboboxSearch.json'
import ComboboxTarget from './ComboboxTarget.json'
import Container from './Container.json'
import CopyButton from './CopyButton.json'
import Dialog from './Dialog.json'
import Divider from './Divider.json'
import Drawer from './Drawer.json'
import Fieldset from './Fieldset.json'
import FileButton from './FileButton.json'
import FileInput from './FileInput.json'
import FloatingIndicator from './FloatingIndicator.json'
import FocusTrap from './FocusTrap.json'
import Grid from './Grid.json'
import GridCol from './GridCol.json'
import Group from './Group.json'
import Highlight from './Highlight.json'
import HoverCard from './HoverCard.json'
import HoverCardTarget from './HoverCardTarget.json'
import Image from './Image.json'
import Indicator from './Indicator.json'
import Input from './Input.json'
import InputDescription from './InputDescription.json'
import InputError from './InputError.json'
import InputLabel from './InputLabel.json'
import InputPlaceholder from './InputPlaceholder.json'
import InputWrapper from './InputWrapper.json'
import JsonInput from './JsonInput.json'
import Kbd from './Kbd.json'
import List from './List.json'
import ListItem from './ListItem.json'
import Loader from './Loader.json'
import LoadingOverlay from './LoadingOverlay.json'
import Mark from './Mark.json'
import Menu from './Menu.json'
import MenuDivider from './MenuDivider.json'
import MenuDropdown from './MenuDropdown.json'
import MenuItem from './MenuItem.json'
import MenuLabel from './MenuLabel.json'
import MenuTarget from './MenuTarget.json'
import Modal from './Modal.json'
import MultiSelect from './MultiSelect.json'
import NativeSelect from './NativeSelect.json'
import NavLink from './NavLink.json'
import Notification from './Notification.json'
import NumberFormatter from './NumberFormatter.json'
import NumberInput from './NumberInput.json'
import Overlay from './Overlay.json'
import Pagination from './Pagination.json'
import PaginationControl from './PaginationControl.json'
import PaginationDots from './PaginationDots.json'
import PaginationFirst from './PaginationFirst.json'
import PaginationItems from './PaginationItems.json'
import PaginationLast from './PaginationLast.json'
import PaginationNext from './PaginationNext.json'
import PaginationPrevious from './PaginationPrevious.json'
import PaginationRoot from './PaginationRoot.json'
import Paper from './Paper.json'
import PasswordInput from './PasswordInput.json'
import Pill from './Pill.json'
import PillGroup from './PillGroup.json'
import PillsInput from './PillsInput.json'
import PillsInputField from './PillsInputField.json'
import PinInput from './PinInput.json'
import Popover from './Popover.json'
import PopoverTarget from './PopoverTarget.json'
import Portal from './Portal.json'
import Progress from './Progress.json'
import ProgressRoot from './ProgressRoot.json'
import ProgressSection from './ProgressSection.json'
import Radio from './Radio.json'
import RadioCard from './RadioCard.json'
import RadioGroup from './RadioGroup.json'
import RadioIndicator from './RadioIndicator.json'
import RangeSlider from './RangeSlider.json'
import Rating from './Rating.json'
import RingProgress from './RingProgress.json'
import ScrollArea from './ScrollArea.json'
import SegmentedControl from './SegmentedControl.json'
import Select from './Select.json'
import SemiCircleProgress from './SemiCircleProgress.json'
import SimpleGrid from './SimpleGrid.json'
import Skeleton from './Skeleton.json'
import Slider from './Slider.json'
import Space from './Space.json'
import Spoiler from './Spoiler.json'
import Stack from './Stack.json'
import Stepper from './Stepper.json'
import StepperStep from './StepperStep.json'
import Switch from './Switch.json'
import SwitchGroup from './SwitchGroup.json'
import Table from './Table.json'
import Tabs from './Tabs.json'
import TabsList from './TabsList.json'
import TabsPanel from './TabsPanel.json'
import TabsTab from './TabsTab.json'
import TagsInput from './TagsInput.json'
import Text from './Text.json'
import TextInput from './TextInput.json'
import Textarea from './Textarea.json'
import ThemeIcon from './ThemeIcon.json'
import Timeline from './Timeline.json'
import TimelineItem from './TimelineItem.json'
import Title from './Title.json'
import Tooltip from './Tooltip.json'
import Transition from './Transition.json'
import Tree from './Tree.json'
import TypographyStylesProvider from './TypographyStylesProvider.json'
import UnstyledButton from './UnstyledButton.json'
import VisuallyHidden from './VisuallyHidden.json'

export function getPropsMap() {
  return [
    Accordion,
    AccordionControl,
    AccordionItem,
    AccordionLabel,
    AccordionPanel,
    ActionIcon,
    ActionIconGroup,
    Affix,
    Alert,
    Anchor,
    AppShell,
    AppShellAside,
    AppShellFooter,
    AppShellHeader,
    AppShellNavbar,
    AspectRatio,
    Autocomplete,
    Avatar,
    BackgroundImage,
    Badge,
    Blockquote,
    Box,
    Breadcrumbs,
    Burger,
    Button,
    ButtonGroup,
    Card,
    CardSection,
    Center,
    Checkbox,
    CheckboxCard,
    CheckboxGroup,
    CheckboxIndicator,
    Chip,
    ChipGroup,
    CloseButton,
    Code,
    Collapse,
    ColorInput,
    ColorPicker,
    ColorSwatch,
    Combobox,
    ComboboxDropdown,
    ComboboxDropdownTarget,
    ComboboxEventsTarget,
    ComboboxGroup,
    ComboboxOption,
    ComboboxSearch,
    ComboboxTarget,
    Container,
    CopyButton,
    Dialog,
    Divider,
    Drawer,
    Fieldset,
    FileButton,
    FileInput,
    FloatingIndicator,
    FocusTrap,
    Grid,
    GridCol,
    Group,
    Highlight,
    HoverCard,
    HoverCardTarget,
    Image,
    Indicator,
    Input,
    InputDescription,
    InputError,
    InputLabel,
    InputPlaceholder,
    InputWrapper,
    JsonInput,
    Kbd,
    List,
    ListItem,
    Loader,
    LoadingOverlay,
    Mark,
    Menu,
    MenuDivider,
    MenuDropdown,
    MenuItem,
    MenuLabel,
    MenuTarget,
    Modal,
    MultiSelect,
    NativeSelect,
    NavLink,
    Notification,
    NumberFormatter,
    NumberInput,
    Overlay,
    Pagination,
    PaginationControl,
    PaginationDots,
    PaginationFirst,
    PaginationItems,
    PaginationLast,
    PaginationNext,
    PaginationPrevious,
    PaginationRoot,
    Paper,
    PasswordInput,
    Pill,
    PillGroup,
    PillsInput,
    PillsInputField,
    PinInput,
    Popover,
    PopoverTarget,
    Portal,
    Progress,
    ProgressRoot,
    ProgressSection,
    Radio,
    RadioCard,
    RadioGroup,
    RadioIndicator,
    RangeSlider,
    Rating,
    RingProgress,
    ScrollArea,
    SegmentedControl,
    Select,
    SemiCircleProgress,
    SimpleGrid,
    Skeleton,
    Slider,
    Space,
    Spoiler,
    Stack,
    Stepper,
    StepperStep,
    Switch,
    SwitchGroup,
    Table,
    Tabs,
    TabsList,
    TabsPanel,
    TabsTab,
    TagsInput,
    Text,
    TextInput,
    Textarea,
    ThemeIcon,
    Timeline,
    TimelineItem,
    Title,
    Tooltip,
    Transition,
    Tree,
    TypographyStylesProvider,
    UnstyledButton,
    VisuallyHidden,
  ]
}

export function getComponentsMap() {
  return [
    [Accordion, Accordion.name, `<${Accordion.name}></${Accordion.name}`],
    [AccordionControl, AccordionControl.name, `<${AccordionControl.name}></${AccordionControl.name}`],
    [AccordionItem, AccordionItem.name, `<${AccordionItem.name}></${AccordionItem.name}`],
    [AccordionLabel, AccordionLabel.name, `<${AccordionLabel.name}></${AccordionLabel.name}`],
    [AccordionPanel, AccordionPanel.name, `<${AccordionPanel.name}></${AccordionPanel.name}`],
    [ActionIcon, ActionIcon.name, `<${ActionIcon.name}></${ActionIcon.name}`],
    [ActionIconGroup, ActionIconGroup.name, `<${ActionIconGroup.name}></${ActionIconGroup.name}`],
    [Affix, Affix.name, `<${Affix.name}></${Affix.name}`],
    [Alert, Alert.name, `<${Alert.name}></${Alert.name}`],
    [Anchor, Anchor.name, `<${Anchor.name}></${Anchor.name}`],
    [AppShell, AppShell.name, `<${AppShell.name}></${AppShell.name}`],
    [AppShellAside, AppShellAside.name, `<${AppShellAside.name}></${AppShellAside.name}`],
    [AppShellFooter, AppShellFooter.name, `<${AppShellFooter.name}></${AppShellFooter.name}`],
    [AppShellHeader, AppShellHeader.name, `<${AppShellHeader.name}></${AppShellHeader.name}`],
    [AppShellNavbar, AppShellNavbar.name, `<${AppShellNavbar.name}></${AppShellNavbar.name}`],
    [AspectRatio, AspectRatio.name, `<${AspectRatio.name}></${AspectRatio.name}`],
    [Autocomplete, Autocomplete.name, `<${Autocomplete.name}></${Autocomplete.name}`],
    [Avatar, Avatar.name, `<${Avatar.name}></${Avatar.name}`],
    [BackgroundImage, BackgroundImage.name, `<${BackgroundImage.name}></${BackgroundImage.name}`],
    [Badge, Badge.name, `<${Badge.name}></${Badge.name}`],
    [Blockquote, Blockquote.name, `<${Blockquote.name}></${Blockquote.name}`],
    [Box, Box.name, `<${Box.name}></${Box.name}`],
    [Breadcrumbs, Breadcrumbs.name, `<${Breadcrumbs.name}></${Breadcrumbs.name}`],
    [Burger, Burger.name, `<${Burger.name}></${Burger.name}`],
    [Button, Button.name, `<${Button.name}></${Button.name}`],
    [ButtonGroup, ButtonGroup.name, `<${ButtonGroup.name}></${ButtonGroup.name}`],
    [Card, Card.name, `<${Card.name}></${Card.name}`],
    [CardSection, CardSection.name, `<${CardSection.name}></${CardSection.name}`],
    [Center, Center.name, `<${Center.name}></${Center.name}`],
    [Checkbox, Checkbox.name, `<${Checkbox.name}></${Checkbox.name}`],
    [CheckboxCard, CheckboxCard.name, `<${CheckboxCard.name}></${CheckboxCard.name}`],
    [CheckboxGroup, CheckboxGroup.name, `<${CheckboxGroup.name}></${CheckboxGroup.name}`],
    [CheckboxIndicator, CheckboxIndicator.name, `<${CheckboxIndicator.name}></${CheckboxIndicator.name}`],
    [Chip, Chip.name, `<${Chip.name}></${Chip.name}`],
    [ChipGroup, ChipGroup.name, `<${ChipGroup.name}></${ChipGroup.name}`],
    [CloseButton, CloseButton.name, `<${CloseButton.name}></${CloseButton.name}`],
    [Code, Code.name, `<${Code.name}></${Code.name}`],
    [Collapse, Collapse.name, `<${Collapse.name}></${Collapse.name}`],
    [ColorInput, ColorInput.name, `<${ColorInput.name}></${ColorInput.name}`],
    [ColorPicker, ColorPicker.name, `<${ColorPicker.name}></${ColorPicker.name}`],
    [ColorSwatch, ColorSwatch.name, `<${ColorSwatch.name}></${ColorSwatch.name}`],
    [Combobox, Combobox.name, `<${Combobox.name}></${Combobox.name}`],
    [ComboboxDropdown, ComboboxDropdown.name, `<${ComboboxDropdown.name}></${ComboboxDropdown.name}`],
    [ComboboxDropdownTarget, ComboboxDropdownTarget.name, `<${ComboboxDropdownTarget.name}></${ComboboxDropdownTarget.name}`],
    [ComboboxEventsTarget, ComboboxEventsTarget.name, `<${ComboboxEventsTarget.name}></${ComboboxEventsTarget.name}`],
    [ComboboxGroup, ComboboxGroup.name, `<${ComboboxGroup.name}></${ComboboxGroup.name}`],
    [ComboboxOption, ComboboxOption.name, `<${ComboboxOption.name}></${ComboboxOption.name}`],
    [ComboboxSearch, ComboboxSearch.name, `<${ComboboxSearch.name}></${ComboboxSearch.name}`],
    [ComboboxTarget, ComboboxTarget.name, `<${ComboboxTarget.name}></${ComboboxTarget.name}`],
    [Container, Container.name, `<${Container.name}></${Container.name}`],
    [CopyButton, CopyButton.name, `<${CopyButton.name}></${CopyButton.name}`],
    [Dialog, Dialog.name, `<${Dialog.name}></${Dialog.name}`],
    [Divider, Divider.name, `<${Divider.name}></${Divider.name}`],
    [Drawer, Drawer.name, `<${Drawer.name}></${Drawer.name}`],
    [Fieldset, Fieldset.name, `<${Fieldset.name}></${Fieldset.name}`],
    [FileButton, FileButton.name, `<${FileButton.name}></${FileButton.name}`],
    [FileInput, FileInput.name, `<${FileInput.name}></${FileInput.name}`],
    [FloatingIndicator, FloatingIndicator.name, `<${FloatingIndicator.name}></${FloatingIndicator.name}`],
    [FocusTrap, FocusTrap.name, `<${FocusTrap.name}></${FocusTrap.name}`],
    [Grid, Grid.name, `<${Grid.name}></${Grid.name}`],
    [GridCol, GridCol.name, `<${GridCol.name}></${GridCol.name}`],
    [Group, Group.name, `<${Group.name}></${Group.name}`],
    [Highlight, Highlight.name, `<${Highlight.name}></${Highlight.name}`],
    [HoverCard, HoverCard.name, `<${HoverCard.name}></${HoverCard.name}`],
    [HoverCardTarget, HoverCardTarget.name, `<${HoverCardTarget.name}></${HoverCardTarget.name}`],
    [Image, Image.name, `<${Image.name}></${Image.name}`],
    [Indicator, Indicator.name, `<${Indicator.name}></${Indicator.name}`],
    [Input, Input.name, `<${Input.name}></${Input.name}`],
    [InputDescription, InputDescription.name, `<${InputDescription.name}></${InputDescription.name}`],
    [InputError, InputError.name, `<${InputError.name}></${InputError.name}`],
    [InputLabel, InputLabel.name, `<${InputLabel.name}></${InputLabel.name}`],
    [InputPlaceholder, InputPlaceholder.name, `<${InputPlaceholder.name}></${InputPlaceholder.name}`],
    [InputWrapper, InputWrapper.name, `<${InputWrapper.name}></${InputWrapper.name}`],
    [JsonInput, JsonInput.name, `<${JsonInput.name}></${JsonInput.name}`],
    [Kbd, Kbd.name, `<${Kbd.name}></${Kbd.name}`],
    [List, List.name, `<${List.name}></${List.name}`],
    [ListItem, ListItem.name, `<${ListItem.name}></${ListItem.name}`],
    [Loader, Loader.name, `<${Loader.name}></${Loader.name}`],
    [LoadingOverlay, LoadingOverlay.name, `<${LoadingOverlay.name}></${LoadingOverlay.name}`],
    [Mark, Mark.name, `<${Mark.name}></${Mark.name}`],
    [Menu, Menu.name, `<${Menu.name}></${Menu.name}`],
    [MenuDivider, MenuDivider.name, `<${MenuDivider.name}></${MenuDivider.name}`],
    [MenuDropdown, MenuDropdown.name, `<${MenuDropdown.name}></${MenuDropdown.name}`],
    [MenuItem, MenuItem.name, `<${MenuItem.name}></${MenuItem.name}`],
    [MenuLabel, MenuLabel.name, `<${MenuLabel.name}></${MenuLabel.name}`],
    [MenuTarget, MenuTarget.name, `<${MenuTarget.name}></${MenuTarget.name}`],
    [Modal, Modal.name, `<${Modal.name}></${Modal.name}`],
    [MultiSelect, MultiSelect.name, `<${MultiSelect.name}></${MultiSelect.name}`],
    [NativeSelect, NativeSelect.name, `<${NativeSelect.name}></${NativeSelect.name}`],
    [NavLink, NavLink.name, `<${NavLink.name}></${NavLink.name}`],
    [Notification, Notification.name, `<${Notification.name}></${Notification.name}`],
    [NumberFormatter, NumberFormatter.name, `<${NumberFormatter.name}></${NumberFormatter.name}`],
    [NumberInput, NumberInput.name, `<${NumberInput.name}></${NumberInput.name}`],
    [Overlay, Overlay.name, `<${Overlay.name}></${Overlay.name}`],
    [Pagination, Pagination.name, `<${Pagination.name}></${Pagination.name}`],
    [PaginationControl, PaginationControl.name, `<${PaginationControl.name}></${PaginationControl.name}`],
    [PaginationDots, PaginationDots.name, `<${PaginationDots.name}></${PaginationDots.name}`],
    [PaginationFirst, PaginationFirst.name, `<${PaginationFirst.name}></${PaginationFirst.name}`],
    [PaginationItems, PaginationItems.name, `<${PaginationItems.name}></${PaginationItems.name}`],
    [PaginationLast, PaginationLast.name, `<${PaginationLast.name}></${PaginationLast.name}`],
    [PaginationNext, PaginationNext.name, `<${PaginationNext.name}></${PaginationNext.name}`],
    [PaginationPrevious, PaginationPrevious.name, `<${PaginationPrevious.name}></${PaginationPrevious.name}`],
    [PaginationRoot, PaginationRoot.name, `<${PaginationRoot.name}></${PaginationRoot.name}`],
    [Paper, Paper.name, `<${Paper.name}></${Paper.name}`],
    [PasswordInput, PasswordInput.name, `<${PasswordInput.name}></${PasswordInput.name}`],
    [Pill, Pill.name, `<${Pill.name}></${Pill.name}`],
    [PillGroup, PillGroup.name, `<${PillGroup.name}></${PillGroup.name}`],
    [PillsInput, PillsInput.name, `<${PillsInput.name}></${PillsInput.name}`],
    [PillsInputField, PillsInputField.name, `<${PillsInputField.name}></${PillsInputField.name}`],
    [PinInput, PinInput.name, `<${PinInput.name}></${PinInput.name}`],
    [Popover, Popover.name, `<${Popover.name}></${Popover.name}`],
    [PopoverTarget, PopoverTarget.name, `<${PopoverTarget.name}></${PopoverTarget.name}`],
    [Portal, Portal.name, `<${Portal.name}></${Portal.name}`],
    [Progress, Progress.name, `<${Progress.name}></${Progress.name}`],
    [ProgressRoot, ProgressRoot.name, `<${ProgressRoot.name}></${ProgressRoot.name}`],
    [ProgressSection, ProgressSection.name, `<${ProgressSection.name}></${ProgressSection.name}`],
    [Radio, Radio.name, `<${Radio.name}></${Radio.name}`],
    [RadioCard, RadioCard.name, `<${RadioCard.name}></${RadioCard.name}`],
    [RadioGroup, RadioGroup.name, `<${RadioGroup.name}></${RadioGroup.name}`],
    [RadioIndicator, RadioIndicator.name, `<${RadioIndicator.name}></${RadioIndicator.name}`],
    [RangeSlider, RangeSlider.name, `<${RangeSlider.name}></${RangeSlider.name}`],
    [Rating, Rating.name, `<${Rating.name}></${Rating.name}`],
    [RingProgress, RingProgress.name, `<${RingProgress.name}></${RingProgress.name}`],
    [ScrollArea, ScrollArea.name, `<${ScrollArea.name}></${ScrollArea.name}`],
    [SegmentedControl, SegmentedControl.name, `<${SegmentedControl.name}></${SegmentedControl.name}`],
    [Select, Select.name, `<${Select.name}></${Select.name}`],
    [SemiCircleProgress, SemiCircleProgress.name, `<${SemiCircleProgress.name}></${SemiCircleProgress.name}`],
    [SimpleGrid, SimpleGrid.name, `<${SimpleGrid.name}></${SimpleGrid.name}`],
    [Skeleton, Skeleton.name, `<${Skeleton.name}></${Skeleton.name}`],
    [Slider, Slider.name, `<${Slider.name}></${Slider.name}`],
    [Space, Space.name, `<${Space.name}></${Space.name}`],
    [Spoiler, Spoiler.name, `<${Spoiler.name}></${Spoiler.name}`],
    [Stack, Stack.name, `<${Stack.name}></${Stack.name}`],
    [Stepper, Stepper.name, `<${Stepper.name}></${Stepper.name}`],
    [StepperStep, StepperStep.name, `<${StepperStep.name}></${StepperStep.name}`],
    [Switch, Switch.name, `<${Switch.name}></${Switch.name}`],
    [SwitchGroup, SwitchGroup.name, `<${SwitchGroup.name}></${SwitchGroup.name}`],
    [Table, Table.name, `<${Table.name}></${Table.name}`],
    [Tabs, Tabs.name, `<${Tabs.name}></${Tabs.name}`],
    [TabsList, TabsList.name, `<${TabsList.name}></${TabsList.name}`],
    [TabsPanel, TabsPanel.name, `<${TabsPanel.name}></${TabsPanel.name}`],
    [TabsTab, TabsTab.name, `<${TabsTab.name}></${TabsTab.name}`],
    [TagsInput, TagsInput.name, `<${TagsInput.name}></${TagsInput.name}`],
    [Text, Text.name, `<${Text.name}></${Text.name}`],
    [TextInput, TextInput.name, `<${TextInput.name}></${TextInput.name}`],
    [Textarea, Textarea.name, `<${Textarea.name}></${Textarea.name}`],
    [ThemeIcon, ThemeIcon.name, `<${ThemeIcon.name}></${ThemeIcon.name}`],
    [Timeline, Timeline.name, `<${Timeline.name}></${Timeline.name}`],
    [TimelineItem, TimelineItem.name, `<${TimelineItem.name}></${TimelineItem.name}`],
    [Title, Title.name, `<${Title.name}></${Title.name}`],
    [Tooltip, Tooltip.name, `<${Tooltip.name}></${Tooltip.name}`],
    [Transition, Transition.name, `<${Transition.name}></${Transition.name}`],
    [Tree, Tree.name, `<${Tree.name}></${Tree.name}`],
    [TypographyStylesProvider, TypographyStylesProvider.name, `<${TypographyStylesProvider.name}></${TypographyStylesProvider.name}`],
    [UnstyledButton, UnstyledButton.name, `<${UnstyledButton.name}></${UnstyledButton.name}`],
    [VisuallyHidden, VisuallyHidden.name, `<${VisuallyHidden.name}></${VisuallyHidden.name}`],
  ]
}